<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="http://xmlns.jcp.org/xml/ns/javaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/javaee
         http://xmlns.jcp.org/xml/ns/javaee/web-app_4_0.xsd"
         version="4.0">

    <display-name>Laptop4Study - Computer Store</display-name>

    <!-- ======================== -->
    <!-- 🔹 TRANG MẶC ĐỊNH -->
    <!-- ======================== -->
    <welcome-file-list>
        <welcome-file>index.jsp</welcome-file>
    </welcome-file-list>

    <!-- ======================== -->
    <!-- 🔹 ENCODING UTF-8 (TOÀN SYSTEM) -->
    <!-- ======================== -->
    <filter>
        <filter-name>EncodingFilter</filter-name>
        <filter-class>org.apache.catalina.filters.SetCharacterEncodingFilter</filter-class>
        <init-param>
            <param-name>encoding</param-name>
            <param-value>UTF-8</param-value>
        </init-param>
        <init-param>
            <param-name>ignore</param-name>
            <param-value>false</param-value>
        </init-param>
    </filter>
    <filter-mapping>
        <filter-name>EncodingFilter</filter-name>
        <url-pattern>/*</url-pattern>
    </filter-mapping>

    <!-- ======================== -->
    <!-- 🔹 BỘ LỌC QUYỀN TRUY CẬP ADMIN -->
    <!-- ======================== -->
    <filter>
        <filter-name>AdminFilter</filter-name>
        <filter-class>servlet.AdminFilter</filter-class>
    </filter>
    <filter-mapping>
        <filter-name>AdminFilter</filter-name>
        <url-pattern>/admin/*</url-pattern>
    </filter-mapping>
    <filter-mapping>
        <filter-name>AdminFilter</filter-name>
        <url-pattern>/api/admin/*</url-pattern>
    </filter-mapping>

    <!-- ======================== -->
    <!-- 🔹 LOGIN -->
    <!-- ======================== -->
    <servlet>
        <servlet-name>LoginServlet</servlet-name>
        <servlet-class>servlet.LoginServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>LoginServlet</servlet-name>
        <url-pattern>/login</url-pattern>
    </servlet-mapping>

    <!-- ======================== -->
    <!-- 🔹 TEST DB -->
    <!-- ======================== -->
    <servlet>
        <servlet-name>TestDBServlet</servlet-name>
        <servlet-class>servlet.TestDBServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>TestDBServlet</servlet-name>
        <url-pattern>/testdb</url-pattern>
    </servlet-mapping>

    <!-- ======================== -->
    <!-- 🔹 LOGOUT -->
    <!-- ======================== -->
    <servlet>
        <servlet-name>LogoutServlet</servlet-name>
        <servlet-class>servlet.LogoutServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>LogoutServlet</servlet-name>
        <url-pattern>/logout</url-pattern>
    </servlet-mapping>

    <!-- ======================== -->
    <!-- 🔹 REGISTER -->
    <!-- ======================== -->
    <servlet>
        <servlet-name>RegisterServlet</servlet-name>
        <servlet-class>servlet.RegisterServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>RegisterServlet</servlet-name>
        <url-pattern>/register</url-pattern>
    </servlet-mapping>

    <!-- ======================== -->
    <!-- 🔹 PRODUCT API (CHO NGƯỜI DÙNG) -->
    <!-- ======================== -->
    <servlet>
        <servlet-name>ProductServlet</servlet-name>
        <servlet-class>servlet.ProductServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>ProductServlet</servlet-name>
        <url-pattern>/api/products</url-pattern>
    </servlet-mapping>

    <!-- ======================== -->
    <!-- 🔹 PRODUCT CRUD API (ADMIN) -->
    <!-- ======================== -->
    <servlet>
        <servlet-name>AdminProductServlet</servlet-name>
        <servlet-class>servlet.AdminProductServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>AdminProductServlet</servlet-name>
        <url-pattern>/api/admin/products</url-pattern>
    </servlet-mapping>

    <!-- ======================== -->
    <!-- 🔹 ADMIN MAIN PAGE -->
    <!-- ======================== -->
    <servlet>
        <servlet-name>AdminPageServlet</servlet-name>
        <servlet-class>servlet.AdminPageServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>AdminPageServlet</servlet-name>
        <url-pattern>/admin</url-pattern>
    </servlet-mapping>

    <!-- ======================== -->
    <!-- 🔹 ADMIN PRODUCT PAGE -->
    <!-- ======================== -->
    <servlet>
        <servlet-name>AdminProductPageServlet</servlet-name>
        <servlet-class>servlet.AdminProductPageServlet</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>AdminProductPageServlet</servlet-name>
        <url-pattern>/admin/products</url-pattern>
    </servlet-mapping>

    <!-- ======================== -->
    <!-- 🔹 PAGE CONTROLLER (HOME...) -->
    <!-- ======================== -->
    <servlet>
        <servlet-name>PageController</servlet-name>
        <servlet-class>servlet.PageController</servlet-class>
    </servlet>
    <servlet-mapping>
        <servlet-name>PageController</servlet-name>
        <url-pattern>/home</url-pattern>
    </servlet-mapping>
    <servlet-mapping>
        <servlet-name>PageController</servlet-name>
        <url-pattern>/page/*</url-pattern>
    </servlet-mapping>
    <servlet-mapping>
        <servlet-name>PageController</servlet-name>
        <url-pattern>/giohang</url-pattern>
    </servlet-mapping>

    <!-- ======================== -->
    <!-- 🔹SESSION CONFIG -->
    <!-- ======================== -->
    <session-config>
        <session-timeout>1</session-timeout>
    </session-config>

    <!-- ======================== -->
    <!-- 🔹 ERROR PAGES -->
    <!-- ======================== -->
    <error-page>
        <error-code>404</error-code>
        <location>/WEB-INF/views/error/404.jsp</location>
    </error-page>

    <error-page>
        <error-code>500</error-code>
        <location>/WEB-INF/views/error/500.jsp</location>
    </error-page>

</web-app>
